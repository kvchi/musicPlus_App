{
  "name": "musicplus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "npm --prefix musicPlus run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix musicPlus run build",
    "lint": "npm --prefix musicPlus run lint",
    "start:server": "npm --prefix server start"
  }
}
